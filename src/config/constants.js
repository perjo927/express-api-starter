const ENVS = {
  PRODUCTION: "production",
  DEVELOPMENT: "development",
  TEST: "test",
};

const ROUTES = {
  TEST_AUTH: "/test-auth",
  PING: "/ping",
  AUTHORIZE: "/authorize",
};

module.exports = {
  ENVS,
  ROUTES,
};
